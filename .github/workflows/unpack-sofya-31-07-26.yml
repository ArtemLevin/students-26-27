name: Archived Sofya module compatibility check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Compatibility fix is already applied
        run: echo "students/sofya_kalney/site/31.07.26.html now contains direct offline HTML"
