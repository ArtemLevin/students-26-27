window.__NIKOL_PAGE_PARTS.push("Тёмная тема';const dt=safe.get('credit-detail')||'full';document.documentElement.dataset.detail=dt;$('#detailSelect').value=dt;const ti=Number(safe.get('credit-task'));if(Number.isInteger(ti)&&ti>=0&&ti<tasks.length)state.task=ti;const st=Number(safe.get(`credit-step-${state.task}`));state.step=Number.isFinite(st)?st:0;const tab=safe.get('credit-tab');if(tab&&$('#panel-'+tab))state.tab=tab;let checks=[];try{checks=JSON.parse(safe.get('credit-checks')||'[]')}catch(e){}$$('#checklist input').forEach(x=>x.checked=checks.includes(x.value));taskPicker();applyTask();printTasks();setTab(state.tab);ready();renderAll()}init();\n</script>\n</body></html>\n");
